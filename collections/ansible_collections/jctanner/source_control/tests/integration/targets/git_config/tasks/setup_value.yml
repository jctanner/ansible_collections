---
# ------
# set up : set jctanner.source_control.gitconfig with value
- name: set up with value
  copy:
    src: jctanner.source_control.gitconfig
    dest: ~/.jctanner.source_control.gitconfig
...